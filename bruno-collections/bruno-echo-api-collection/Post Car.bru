meta {
  name: Post Car
  type: http
  seq: 3
}

post {
  url: https://echo.usebruno.com
  body: json
  auth: inherit
}

body:json {
  {
    "ID": "{{$randomUUID}}",
    "Product Name": "{{$randomProductName}}",
    "Product Price": "{{$randomPrice}}"
  }
}

assert {
  res.status: eq 200
  res.body.ID.length: eq 36
}

settings {
  encodeUrl: true
}

meta {
  name: User Info
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/usebruno
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("status must be 200", function() {
    expect(res.status).to.eql(200);
  });
}
